<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastro de Usuário</title>
  <base href="/cadastro">
  <link rel="stylesheet" href="/style_login.css">
</head>
<body>
  <div class="page">
    <form method="post" action="/cadastro" class="formLogin" onsubmit="alert('Usuário cadastrado com sucesso!')">
      <h1>Cadastro de Usuário</h1>
      <p>Digite os seus dados no campo abaixo.</p>
        <section>
          <div class="pai">
            <div>
              <label for="nome" id="atv">Nome</label>
              <input type="text" name="nome" placeholder="Digite o seu nome" autofocus="true" id="atv" required>
            </div>

            <div>
              <label for="cpf" id="atv">CPF</label>
              <input type="text" name="cpf" placeholder="Digite o seu cpf" id="atv" required>
            </div>
          </div>

         <div class="pai">
            <div>
              <label for="email" id="atv">E-mail</label>
              <input type="email" name="email" placeholder="Digite o seu e-mail" id="atv" required>
            </div>

            <div>
              <label for="senha" id="atv">Senha</label>
              <input type="password" name="senha" placeholder="Digite uma senha" id="atv" required>
            </div>
          </div>

          <div class="pai">
            <div>
              <label for="telefone" id="atv">Telefone</label>
              <input type="text" name="telefone" placeholder="Digite o seu telefone" id="atv" required>
            </div>

            <div>
              <label for="endereco" id="atv">Endereco</label>
              <input type="text" name="endereco" id="enderecoInput atv" placeholder="Digite o seu endereço" required>
            </div>
          </div>

          <div class="pai">
            <div>
              <label for="cidade" id="atv">Cidade</label>
              <input type="text" name="cidade" id="cidadeInput atv" placeholder="Digite a sua cidade" required>
            </div>

            <div>
              <label for="pais" id="atv">País</label>
            <input type="text" name="pais" id="paisInput atv" placeholder="Digite o seu país" required>
            </div>
          </div>
        </section>

        <section id="botoes">
          <button type="reset" value="Acessar" class="cadastro">Apagar</button>
          <button type="submit" value="Acessar" class="cadastro" id="env">Enviar</button>
        </section>
    </form>
  </div>
  <!-- <script src="https://unpkg.com/cep-promise/dist/cep-promise.js"></script>
  <script>
    const cepInput = document.querySelector('#enderecoInput');
    const cidadeInput = document.querySelector('#cidadeInput');
    const paisInput = document.querySelector('#paisInput');

    cepInput.addEventListener('change', function() {
      const cep = cepInput.value;

      window.cepPromise(cep)
        .then(function(result) {
          cidadeInput.value = result.city;
          paisInput.value = result.state;
        })
        .catch(function(error) {
          console.log(error);
        });
    });
  </script> -->
</body>
</html>